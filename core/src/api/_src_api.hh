#ifndef WEBVIEW_SRC_API_HH
#define WEBVIEW_SRC_API_HH

#if defined(__cplusplus) && !defined(WEBVIEW_HEADER)
#include "api.cc"

#endif // #if defined(__cplusplus) && !defined(WEBVIEW_HEADER)
#endif // WEBVIEW_SRC_API_HH
